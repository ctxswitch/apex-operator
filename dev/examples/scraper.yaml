apiVersion: apex.ctx.sh/v1
kind: Scraper
metadata:
  name: scraper
  labels:
    app: scraper
    service: apex
spec:
  selector:
    matchLabels:
      # k8s-app: kube-proxy
  annotationPrefix: apex.ctx.sh
  output:
    logger:
